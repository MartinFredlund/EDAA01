package resizable;

public interface Resizable {
	/**
	 * Scales down the object with scaleFactor.
	 * 
	 * @param scaleFactor
	 *            the scale factor which is used to reduce the size of the
	 *            object
	 */
	void downSize(int scaleFactor);
}